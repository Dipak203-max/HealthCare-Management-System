<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8" isELIgnored="false"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core"%>

<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Doctor Management</title>
<style>
/* Reset & Base Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #eef2f7;
    color: #2c3e50;
}

/* Layout */
.dashboard-container {
    display: grid;
    grid-template-columns: 250px 1fr;
    min-height: 100vh;
}

/* Sidebar */
.sidebar {
    background: linear-gradient(180deg, #2c3e50 0%, #34495e 100%);
    color: white;
    padding: 30px 20px;
}

.sidebar-header {
    text-align: center;
    margin-bottom: 40px;
}
.sidebar-header h2 {
    font-size: 22px;
}

.sidebar-nav .nav-item {
    margin: 15px 0;
    padding: 10px;
    border-radius: 6px;
    transition: background 0.3s ease;
}

.sidebar-nav .nav-item:hover {
    background-color: #3d566e;
}

.sidebar-nav .nav-item.active {
    background-color: #2980b9;
    font-weight: bold;
}

.sidebar-nav .nav-item a {
    color: white;
    text-decoration: none;
    display: block;
}

/* Main Content */
.main-content {
    padding: 30px;
    background-color: #f8f9fb;
}

/* Header */
.header {
    background: linear-gradient(to right, #3498db, #2980b9);
    color: white;
    padding: 20px 30px;
    border-radius: 12px;
    margin-bottom: 30px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.user-info {
    font-size: 14px;
}

.logout-btn {
    background-color: #e74c3c;
    color: white;
    border: none;
    padding: 8px 14px;
    border-radius: 6px;
    font-weight: bold;
    cursor: pointer;
    margin-left: 15px;
}

/* Notification Alerts */
.alert {
    margin-bottom: 20px;
    padding: 12px 16px;
    border-radius: 8px;
    font-weight: 500;
}
.alert-success {
    background-color: #2ecc71;
    color: white;
}
.alert-danger {
    background-color: #e74c3c;
    color: white;
}

/* Add Button */
.add-btn {
    display: inline-block;
    background-color: #3498db;
    color: white;
    padding: 12px 18px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: bold;
    margin-bottom: 25px;
    transition: background 0.3s ease;
}
.add-btn:hover {
    background-color: #2980b9;
}

/* Doctor Table */
.doctor-table {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0 10px;
}

.doctor-table thead {
    background-color: transparent;
}

.doctor-table th {
    text-align: left;
    padding: 12px 18px;
    background-color: #3498db;
    color: white;
    border-top-left-radius: 10px;
    border-top-right-radius: 10px;
}

.doctor-table td {
    padding: 14px 18px;
    background-color: white;
    box-shadow: 0 4px 8px rgba(0,0,0,0.05);
    border-bottom: 1px solid #e0e0e0;
}

.doctor-table tr td:first-child {
    border-top-left-radius: 10px;
    border-bottom-left-radius: 10px;
}
.doctor-table tr td:last-child {
    border-top-right-radius: 10px;
    border-bottom-right-radius: 10px;
}

/* Action Buttons */
.action-btn {
    padding: 6px 12px;
    border-radius: 6px;
    text-decoration: none;
    font-weight: bold;
    font-size: 13px;
    margin-right: 8px;
    display: inline-block;
}
.edit-btn {
    background-color: #2ecc71;
    color: white;
}
.delete-btn {
    background-color: #e74c3c;
    color: white;
}
</style>
</head>
<body>

<c:if test="${not empty message}">
    <div class="alert alert-success">${message}</div>
    <c:remove var="message" scope="session" />
</c:if>

<c:if test="${not empty error}">
    <div class="alert alert-danger">${error}</div>
    <c:remove var="error" scope="session" />
</c:if>

<div class="dashboard-container">
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="sidebar-header">
            <h2>Healthcare Admin</h2>
        </div>
        <nav class="sidebar-nav">
            <div class="nav-item"><a href="${pageContext.request.contextPath}/DashboardServlet">Dashboard</a></div>
            <div class="nav-item active">Doctors</div>
            <div class="nav-item"><a href="${pageContext.request.contextPath}/PatientListServlet">Patients</a></div>
            <div class="nav-item"><a href="${pageContext.request.contextPath}/AppointmentListServlet">Appointments</a></div>
            <div class="nav-item">Medical Record</div>
            <div class="nav-item">Settings</div>
        </nav>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <div class="header">
            <h3>Doctor Management</h3>
            <div class="user-info">
                Welcome, <strong>${user.username}</strong> (Admin)
                <form action="${pageContext.request.contextPath}/LogoutServlet" method="post" style="display:inline;">
                    <button type="submit" class="logout-btn">Logout</button>
                </form>
            </div>
        </div>

        <a href="${pageContext.request.contextPath}/view/addNewDoctor.jsp" class="add-btn">+ Add New Doctor</a>

        <table class="doctor-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Specialization</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <c:forEach var="doctor" items="${doctors}">
                    <tr>
                        <td>${doctor.doctorId}</td>
                        <td>${doctor.name}</td>
                        <td>${doctor.specialization}</td>
                        <td>
                            <a href="${pageContext.request.contextPath}/EditDoctorServlet?id=${doctor.doctorId}" class="action-btn edit-btn">Edit</a>
                            <form action="${pageContext.request.contextPath}/DeleteDoctorServlet" method="post" style="display:inline;">
                                <input type="hidden" name="id" value="${doctor.doctorId}">
                                <button type="submit" class="action-btn delete-btn" style="border: none;">Delete</button>
                            </form>
                        </td>
                    </tr>
                </c:forEach>
            </tbody>
        </table>
    </div>
</div>

</body>
</html>
