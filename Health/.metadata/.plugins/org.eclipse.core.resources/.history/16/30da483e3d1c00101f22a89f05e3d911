<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8" isELIgnored="false"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core"%>

<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Patient Management</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
:root {
    --primary: #7c3aed;
    --primary-light: #8b5cf6;
    --primary-dark: #6d28d9;
    --secondary: #10b981;
    --accent: #f59e0b;
    --danger: #ef4444;
    --info: #0ea5e9;
    
    --bg-dark: #0f172a;
    --bg-darker: #020617;
    --bg-card: #1e293b;
    --text-primary: #f8fafc;
    --text-secondary: #94a3b8;
    --border-color: #334155;
    --highlight: rgba(124, 58, 237, 0.1);
    
    --sidebar-width: 280px;
    --card-radius: 12px;
    --transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', 'Poppins', -apple-system, BlinkMacSystemFont, sans-serif;
}

body {
    background-color: var(--bg-dark);
    color: var(--text-primary);
    line-height: 1.6;
}

.dashboard-container {
    display: grid;
    grid-template-columns: var(--sidebar-width) 1fr;
    min-height: 100vh;
}

/* Sidebar Styles */
.sidebar {
    background-color: var(--bg-darker);
    padding: 2rem 0;
    border-right: 1px solid var(--border-color);
    position: sticky;
    top: 0;
    height: 100vh;
}

.sidebar-header {
    padding: 0 2rem 2rem;
    border-bottom: 1px solid var(--border-color);
    margin-bottom: 2rem;
}

.sidebar-header h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary-light);
    display: flex;
    align-items: center;
    gap: 10px;
}

.sidebar-header h2 i {
    font-size: 1.8rem;
}

.sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding: 0 1.5rem;
}

.nav-item {
    padding: 0.75rem 1rem;
    border-radius: var(--card-radius);
    transition: var(--transition);
}

.nav-item:hover {
    background-color: var(--highlight);
}

.nav-item a {
    color: var(--text-primary);
    text-decoration: none;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 12px;
    transition: var(--transition);
}

.nav-item a:hover {
    color: var(--primary-light);
}

.nav-item i {
    width: 24px;
    font-size: 1.1rem;
    color: var(--text-secondary);
}

.nav-item.active {
    background-color: var(--highlight);
    border-left: 4px solid var(--primary);
}

.nav-item.active a {
    color: var(--primary-light);
}

.nav-item.active i {
    color: var(--primary-light);
}

/* Main Content Styles */
.main-content {
    padding: 2rem;
}

.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 1.5rem;
    background-color: var(--bg-card);
    border-radius: var(--card-radius);
    margin-bottom: 2rem;
    border: 1px solid var(--border-color);
}

.header h3 {
    font-size: 1.5rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 12px;
}

.header h3 i {
    color: var(--primary-light);
}

.user-info {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.user-info span {
    font-size: 0.95rem;
    color: var(--text-secondary);
}

.user-info strong {
    color: var(--text-primary);
    font-weight: 600;
}

.logout-btn {
    background-color: var(--danger);
    color: white;
    border: none;
    padding: 0.5rem 1rem;
    border-radius: var(--card-radius);
    cursor: pointer;
    font-weight: 500;
    transition: var(--transition);
    display: flex;
    align-items: center;
    gap: 8px;
}

.logout-btn:hover {
    background-color: #dc2626;
}

.logout-btn i {
    font-size: 0.9rem;
}

/* Alert Messages */
.alert {
    padding: 1rem 1.5rem;
    margin-bottom: 2rem;
    border-radius: var(--card-radius);
    display: flex;
    align-items: center;
    gap: 12px;
    border-left: 4px solid transparent;
}

.alert-success {
    background-color: rgba(16, 185, 129, 0.1);
    border-color: var(--secondary);
    color: var(--text-primary);
}

.alert-danger {
    background-color: rgba(239, 68, 68, 0.1);
    border-color: var(--danger);
    color: var(--text-primary);
}

.alert i {
    font-size: 1.2rem;
}

.alert-success i {
    color: var(--secondary);
}

.alert-danger i {
    color: var(--danger);
}

/* Patient Table Styles */
.add-btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 0.75rem 1.5rem;
    background-color: var(--primary);
    color: white;
    text-decoration: none;
    border-radius: var(--card-radius);
    margin-bottom: 1.5rem;
    font-weight: 500;
    transition: var(--transition);
}

.add-btn:hover {
    background-color: var(--primary-dark);
    transform: translateY(-2px);
}

.add-btn i {
    font-size: 0.9rem;
}

.patient-table {
    width: 100%;
    border-collapse: collapse;
    background-color: var(--bg-card);
    border-radius: var(--card-radius);
    overflow: hidden;
    border: 1px solid var(--border-color);
    box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1);
}

.patient-table th {
    padding: 1rem 1.5rem;
    text-align: left;
    background-color: var(--primary);
    color: white;
    font-weight: 600;
    text-transform: uppercase;
    font-size: 0.85rem;
    letter-spacing: 0.5px;
}

.patient-table td {
    padding: 1rem 1.5rem;
    border-bottom: 1px solid var(--border-color);
    color: var(--text-primary);
}

.patient-table tr:last-child td {
    border-bottom: none;
}

.patient-table tr:hover {
    background-color: rgba(255,255,255,0.03);
}

.action-btns {
    display: flex;
    gap: 0.75rem;
}

.action-btn {
    padding: 0.5rem 1rem;
    border: none;
    border-radius: var(--card-radius);
    cursor: pointer;
    text-decoration: none;
    font-size: 0.85rem;
    font-weight: 500;
    transition: var(--transition);
    display: inline-flex;
    align-items: center;
    gap: 6px;
}

.edit-btn {
    background-color: var(--secondary);
    color: white;
}

.edit-btn:hover {
    background-color: #0d9f7a;
}

.delete-btn {
    background-color: var(--danger);
    color: white;
}

.delete-btn:hover {
    background-color: #dc2626;
}

.action-btn i {
    font-size: 0.8rem;
}

.delete-form {
    display: inline;
}

/* Responsive adjustments */
@media (max-width: 1200px) {
    .dashboard-container {
        grid-template-columns: 240px 1fr;
    }
}

@media (max-width: 992px) {
    .dashboard-container {
        grid-template-columns: 1fr;
    }
    
    .sidebar {
        height: auto;
        position: static;
        border-right: none;
        border-bottom: 1px solid var(--border-color);
    }
    
    .sidebar-nav {
        flex-direction: row;
        flex-wrap: wrap;
        padding: 1rem;
    }
    
    .nav-item {
        padding: 0.5rem 0.75rem;
    }
}

@media (max-width: 768px) {
    .header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
    }
    
    .user-info {
        width: 100%;
        justify-content: space-between;
    }
    
    .patient-table {
        display: block;
        overflow-x: auto;
    }
}

@media (max-width: 576px) {
    .main-content {
        padding: 1.5rem;
    }
    
    .action-btns {
        flex-direction: column;
        gap: 0.5rem;
    }
    
    .action-btn {
        width: 100%;
        justify-content: center;
    }
}
</style>
</head>
<body>

    <c:if test="${not empty message}">
        <div class="alert alert-success">
            <i class="fas fa-check-circle"></i> ${message}
        </div>
        <c:remove var="message" scope="session" />
    </c:if>

    <c:if test="${not empty error}">
        <div class="alert alert-danger">
            <i class="fas fa-exclamation-circle"></i> ${error}
        </div>
        <c:remove var="error" scope="session" />
    </c:if>

    <div class="dashboard-container">
        <!-- Sidebar -->
        <div class="sidebar">
            <div class="sidebar-header">
                <h2><i class="fas fa-heart-pulse"></i> MedAdmin</h2>
            </div>
            <nav class="sidebar-nav">
                <div class="nav-item">
                    <a href="${pageContext.request.contextPath}/DashboardServlet">
                        <i class="fas fa-tachometer-alt"></i>
                        <span>Dashboard</span>
                    </a>
                </div>
                <div class="nav-item">
                    <a href="${pageContext.request.contextPath}/DoctorListServlet">
                        <i class="fas fa-user-md"></i>
                        <span>Doctors</span>
                    </a>
                </div>
                <div class="nav-item active">
                    <a href="${pageContext.request.contextPath}/PatientListServlet">
                        <i class="fas fa-procedures"></i>
                        <span>Patients</span>
                    </a>
                </div>
                <div class="nav-item">
                    <a href="${pageContext.request.contextPath}/AppointmentListServlet">
                        <i class="fas fa-calendar-check"></i>
                        <span>Appointments</span>
                    </a>
                </div>
                <div class="nav-item">
                    <a href="#">
                        <i class="fas fa-file-medical"></i>
                        <span>Medical Records</span>
                    </a>
                </div>
                <div class="nav-item">
                    <a href="#">
                        <i class="fas fa-cog"></i>
                        <span>Settings</span>
                    </a>
                </div>
            </nav>
        </div>

        <!-- Main Content -->
        <div class="main-content">
            <header class="header">
                <h3><i class="fas fa-procedures"></i> Patient Management</h3>
                <div class="user-info">
                    <span>Welcome, <strong>${user.username}</strong> (Admin)</span>
                    <form action="${pageContext.request.contextPath}/LogoutServlet" method="post">
                        <button type="submit" class="logout-btn">
                            <i class="fas fa-sign-out-alt"></i> Logout
                        </button>
                    </form>
                </div>
            </header>

            <a href="${pageContext.request.contextPath}/view/addPatient.jsp" class="add-btn">
                <i class="fas fa-plus"></i> Add New Patient
            </a>

            <table class="patient-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Phone</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <c:forEach var="patient" items="${patients}">
                        <tr>
                            <td>${patient.patientId}</td>
                            <td>${patient.name}</td>
                            <td>${patient.phone}</td>
                            <td>
                                <div class="action-btns">
                                    <a href="${pageContext.request.contextPath}/EditPatientServlet?id=${patient.patientId}" 
                                       class="action-btn edit-btn">
                                        <i class="fas fa-edit"></i> Edit
                                    </a>
                                    <form class="delete-form" 
                                          action="${pageContext.request.contextPath}/DeletePatientServlet" 
                                          method="post">
                                        <input type="hidden" name="id" value="${patient.patientId}">
                                        <button type="submit" class="action-btn delete-btn">
                                            <i class="fas fa-trash-alt"></i> Delete
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    </c:forEach>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>